/**
 * Custom hooks globales.
 *
 * Ejemplos de hooks que se pueden implementar aquí:
 *   - useAuth        → estado de autenticación del usuario
 *   - useMediaQuery  → breakpoints responsivos
 *   - useDebounce    → retraso en inputs de búsqueda
 *   - useLocalStorage→ persistencia en localStorage
 */
export {};
